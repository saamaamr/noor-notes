# Noor Notes 1.1.5

Patch release after Snap stable 1.1.4 (revision 23). Published on 2026-09-06 as **1.1.5, revision 24** on stable and edge.

## Fix

The 1.1.4 Dev executable included public Supabase configuration, but the official Snap recipe did not. As a result, the Store build disabled the real Google/email account actions. The Snap recipe now supplies the same public project URL and publishable key at build time. No service-role key or OAuth secret is included, and users must explicitly sign in and unlock a vault before syncing.

## Regression protection

- Snap manifest validation rejects missing account configuration and non-publishable keys.
- A headless `--check-cloud-config` command exercises the production configuration/client path without opening notes, keyring, or a network session.
- Every Snap build/Store smoke path checks the installed binary without environment overrides before publication/promotion.
- CLI tests cover valid, missing, insecure and privileged configuration and ensure keys are not printed.

## Scope

No editor, note format, persistence migration, new dependency, Google Drive or OneDrive configuration change. Sync remains manual and opt-in. Public auth settings confirm Google/email providers enabled; this alone does not prove successful OAuth token exchange or two-device sync. Those live acceptance results remain explicitly tracked in `docs/supabase-staging-verification.md`.

## Local verification — 2026-09-06

- Regression reproduced before the fix: missing Snap public configuration and a headless config diagnostic that could not succeed. Both tests now pass.
- `cargo test --workspace`: 264 passed, zero failed; isolated X11/Cairo Xvfb (1920×1080). Log: `target/release-1.1.5-workspace.log`.
- Dev CLI/identity: 5 passed. Strict all-target/all-feature workspace Clippy, formatting and diff checks passed.
- Snap manifest/build/cadence/tag-release, version metadata, automatic versioning, security workflow and Flatpak manifest contracts passed.
- `cargo audit` and `cargo deny check` passed with the existing allowed dependency warnings; not a warning-free audit.
- Independent code review found no Critical/Important defects. Store artifact and live user acceptance are separate gates, not inferred from these results.
- Maintainer confirmed real Dev login/two-device sync and authorized stable 1.1.5; recorded as user-reported acceptance in `docs/supabase-staging-verification.md`, not automated OAuth proof.
- Dev 1.1.5 rebuilt and installed: 13,681,368 bytes (below the 15,000,000-byte gate); compiled configuration passes without runtime environment overrides. Build/install SHA-256: `b2f1c0e228edbe4709e449bca096e8cd98dd87867bdc6ba1587ea99d3a781abb`. Prior executable backup: `target/dev-before-1.1.5.QJ2LXM/noor-notes-dev`.

## Store publication

- Released source: `6c2c314ad4bb915ebea3c9430898649f63813782`.
- [Release run 34040163529](https://github.com/saamaamr/noor-notes/actions/runs/34040163529): successful build, runtime contract, Snapcraft lint, installed-binary configuration check, edge publication and Store-installed smoke/configuration check.
- [Security run 34040162641](https://github.com/saamaamr/noor-notes/actions/runs/34040162641): successful.
- That run intentionally used `release=edge` while waiting for maintainer acceptance, so its automatic stable job was skipped. After confirmation, `snapcraft release noor-notes 24 latest/stable` successfully promoted the exact tested revision without rebuilding or incrementing to 1.1.6.
- Live `snap info noor-notes` confirmed stable and edge both **1.1.5/revision 24**, amd64, **5.19 MB** compressed Snap.
- Update with `sudo snap refresh noor-notes --stable`. No new GitHub downloadable release/tag was created; those assets remain v1.1.1.
