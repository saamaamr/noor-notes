[package]
name = "noor-sync"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
noor-crypto = { path = "../crypto" }
noor-domain = { path = "../domain" }
noor-storage = { path = "../storage" }
tokio.workspace = true
